JAMES_revision — resubmission package for #2026MS005845
=========================================================

main.tex                   revised manuscript (clean, changes incorporated)
diff.tex                   tracked-changes copy (latexdiff of submitted vs revised)
response_to_reviewers.tex  point-by-point response letter
To compile any one of these in Overleaf: Menu (top left) -> Settings ->
Main document -> select the .tex file you want, then Recompile. Overleaf
always compiles the designated main document, not the file currently open
in the editor.

All three compile standalone with pdflatex, xelatex, or lualatex; the
class/style/bst files and all figures they need are in this folder.

The original submission is untouched in JAMES/.
